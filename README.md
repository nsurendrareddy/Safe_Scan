# Cyber Scanner (Multi-page)
1) `pip install -r requirements.txt`
2) Set env: `export VIRUSTOTAL_API_KEY=YOUR_API_KEY`  (Windows PowerShell: `$env:VIRUSTOTAL_API_KEY='YOUR_API_KEY'`)
3) `python app.py`
4) Open `index.html` in your browser.
